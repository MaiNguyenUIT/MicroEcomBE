package com.example.user_service.dto;

public enum ORDER_STATUS {
        FAIL,
        PENDING,
        CONFIRMED,
        PAID,
        ON_GOING,
        DELIVERED,
        SUCCESS,
        CANCELLED,
}
